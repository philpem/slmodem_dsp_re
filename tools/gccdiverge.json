{
  "t_psd": {
    "finding": 1453,
    "checks": [
      "Psd::process"
    ],
    "why": "x87 excess precision in four1/realfft reaching Psd's decibels. Under -fexcess-precision=fast modern GCC keeps 80-bit intermediates the object narrows to 32 bits mid-butterfly, and by the time the transform reaches a decibel the divergence is up to 0.043 dB on bins near -85 dB, where cancellation dominates. It is NOT closable with a tolerance: the smallest separation between two V90PreFilter reference loops that give DIFFERENT answers is SSD 0.00268 dB^2 (AscendNULL vs AscendNULLCodec2, refLoopsType2), a distance of 0.0518 dB, and 0.043 dB on six points moves a signature by 0.105 dB -- four times the separation, so the excess precision can flip autoSelection's winner. `make period` passes all 160: the period compiler has no divergence here, and it is the tier that decides."
  }
}
