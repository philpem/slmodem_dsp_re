[
 {
  "note": "The other half of `vpcm_run`'s set, and the same source against a different binary.  `t_vpcmguard` supplies NONE of the five `VPcmV34*` entry points, so every one of them is null in it and the unwritten-path guard is on the only path there is; it forks, calls `vpcm_run`, and requires the child to have died of SIGABRT (findings F985, F987).  A guard that returned quietly would leave a `.process` running and carrying nothing, and its output -- a buffer of silence -- is what a modem that correctly transmitted nothing produces, so it has to be WATCHED stopping rather than argued about.  These two mutations are the two ends of that: the stop removed, and the guard never reached at all."
 },
 {
  "label": "the guard returns quietly instead of stopping",
  "find": "\tif (!vpcm_unwritten_soft)\n\t\tabort();\n",
  "replace": "\tif (0)\n\t\tabort();\n",
  "fn": "vpcm_notwritten",
  "equivalent": true,
  "why": "vpcm_notwritten's hard-abort arm is unreachable in this binary: all five VPcmV34* entry points and the seven symbols below VPcmV34Progress are now WRITTEN (finding F7606), so vpcm_run never calls vpcm_notwritten(...) with vpcm_unwritten_soft false -- there is no unresolved callee left below VPcmV34Progress for t_vpcmguard to reach (see the note at the head of test/unit/t_vpcmguard.c and F7606's own re-measurement, which found this row identical to its stored verdict at 9ddfc5bc). Removing the abort from dead code cannot be observed by any call this suite makes."
 },
 {
  "label": "the mute path is always taken, so the guard is never reached",
  "find": "\t\tif (s->mute > 0) {\n",
  "replace": "\t\tif (1) {\n",
  "fn": "vpcm_run"
 }
]
