[
 {
  "note": "The mute half of `vpcm_run`'s set, and the same source against a different binary.  THE GUARD ROW WAS REMOVED because the thing it mutated no longer exists: `vpcm.c`'s `vpcm_notwritten` (findings F985, F987) and the whole `vpcm_unwritten` recorder were ADDED APPARATUS -- the blob's `vpcm_run` calls the five `VPcmV34*` entry points by direct `R_386_PC32` relocation with no null test, and has no `vpcm_unwritten` symbol (tools/dis.py ref/slmodemd/dsplibs.o 0x3e40 0x44be) -- so they were deleted along with the five `if (FUNC == 0)` guards.  What is left is the mute-countdown branch, which is a real arm of the object and is caught here.  `t_vpcmguard` is the binary; `vpcmrun` is the other half of the function."
 },
 {
  "label": "the mute path is always taken, so the guard is never reached",
  "find": "\t\tif (s->mute > 0) {\n",
  "replace": "\t\tif (1) {\n",
  "fn": "vpcm_run"
 }
]
