[
 {
  "note": "V90Demapper::V90Demapper, against t_v90demapctor.  EVERY `find` BELOW IS TEXT THAT APPEARS ONCE IN THE FILE, and that is a rule and not a preference: `printErrorHistogramAndReset` shares this translation unit and iterates the same six constellations, so an anchor taken from the loop header alone would match twice, mutate.py would call the entry UNUSABLE, and UNUSABLE DOES NOT FAIL A RUN (finding F1264).  The loop anchors here carry their body with them for exactly that reason."
 },
 {
  "label": "the detector argument is dropped on the floor",
  "find": "\tadiDetector = adi;",
  "replace": "\t(void)adi;"
 },
 {
  "label": "the detector lands where the parameter block belongs",
  "find": "\tadiDetector = adi;",
  "replace": "\tadiDetector = (V90AutoDigitalImpDetector *)params;"
 },
 {
  "label": "the parameter block is never stored",
  "find": "\tthis->params = params;\n\terrorHistogramCount = 0;",
  "replace": "\t(void)params;\n\terrorHistogramCount = 0;",
  "why": "Re-anchored by finding F7793: the constructor's eight zero stores are written high-to-low, which is the object's own emission order, and the two trailing stores are `params` then `errorHistogramCount`.  The mutation is unchanged."
 },
 {
  "label": "the histogram counter keeps whatever it found",
  "find": "\tthis->params = params;\n\terrorHistogramCount = 0;",
  "replace": "\tthis->params = params;",
  "why": "Re-anchored by finding F7793: the constructor's eight zero stores are written high-to-low, which is the object's own emission order, and the two trailing stores are `params` then `errorHistogramCount`.  The mutation is unchanged."
 },
 {
  "label": "the four-byte array is sized in elements rather than bytes",
  "find": "\tcodes = new unsigned int[levels];",
  "replace": "\tcodes = (unsigned int *)new unsigned char[levels];"
 },
 {
  "label": "the one-byte array is sized four bytes to the element",
  "find": "\tsigns = new unsigned char[levels];",
  "replace": "\tsigns = (unsigned char *)new unsigned int[levels];"
 },
 {
  "label": "the two allocations are stored the other way round",
  "find": "\tcodes = new unsigned int[levels];\n\tsigns = new unsigned char[levels];",
  "replace": "\tsigns = (unsigned char *)new unsigned int[levels];\n\tcodes = (unsigned int *)new unsigned char[levels];"
 },
 {
  "label": "the element count is the byte count",
  "find": "\tsampleCapacity = levels;",
  "replace": "\tsampleCapacity = levels * 4;"
 },
 {
  "label": "the serial decoder at +0x664 is left as the seed found it",
  "find": "\t: signDecoder()",
  "replace": "\t/* signDecoder left uninitialised */"
 },
 {
  "label": "+0x664 is cleared a word wide, taking the alignment with it",
  "find": "\tadiDetector = adi;",
  "replace": "\t*(unsigned int *)&signDecoder = 0;\n\tadiDetector = adi;"
 },
 {
  "label": "the last constellation count is left as the constructor found it",
  "find": "\tfor (i = 0; i < V90DEMAPPER_CONSTELLATIONS; i++)\n\t\tconstellationSize[i] = 0;",
  "replace": "\tfor (i = 0; i + 1 < V90DEMAPPER_CONSTELLATIONS; i++)\n\t\tconstellationSize[i] = 0;"
 },
 {
  "label": "the constellation counts start at one apiece",
  "find": "\tfor (i = 0; i < V90DEMAPPER_CONSTELLATIONS; i++)\n\t\tconstellationSize[i] = 0;",
  "replace": "\tfor (i = 0; i < V90DEMAPPER_CONSTELLATIONS; i++)\n\t\tconstellationSize[i] = 1;"
 },
 {
  "label": "the counts loop takes its bound from the levels rather than the rows",
  "find": "\tfor (i = 0; i < V90DEMAPPER_CONSTELLATIONS; i++)\n\t\tconstellationSize[i] = 0;",
  "replace": "\tfor (i = 0; i < V90DEMAPPER_LEVELS; i++)\n\t\tconstellationSize[i] = 0;"
 },
 {
  "label": "bitsPerFrame keeps its seed",
  "find": "\tword_08 = 0;\n\tbitsPerFrame = 0;",
  "replace": "\tword_08 = 0;",
  "why": "Re-anchored by finding F7793: the constructor's eight zero stores are written high-to-low, which is the object's own emission order, and the two trailing stores are `params` then `errorHistogramCount`.  The mutation is unchanged."
 },
 {
  "label": "sampleCount keeps its seed",
  "find": "\tsampleCapacity = levels;\n\n\tsampleCount = 0;\n",
  "replace": "\tsampleCapacity = levels;\n\n",
  "why": "Re-anchored by finding F7793: the constructor's eight zero stores are written high-to-low, which is the object's own emission order, and the two trailing stores are `params` then `errorHistogramCount`.  The mutation is unchanged."
 },
 {
  "label": "the constructor also empties the two histograms",
  "find": "\tthis->params = params;\n\terrorHistogramCount = 0;",
  "replace": "\tfor (i = 0; i < V90DEMAPPER_CONSTELLATIONS * V90DEMAPPER_LEVELS; i++) {\n\t\terrorSum[0][i] = 0;\n\t\terrorCount[0][i] = 0;\n\t}\n\tthis->params = params;\n\terrorHistogramCount = 0;",
  "why": "Re-anchored by finding F7793: the constructor's eight zero stores are written high-to-low, which is the object's own emission order, and the two trailing stores are `params` then `errorHistogramCount`.  The mutation is unchanged."
 },
 {
  "label": "the constructor also empties the constellation table",
  "find": "\tthis->params = params;\n\terrorHistogramCount = 0;",
  "replace": "\tfor (i = 0; i < V90DEMAPPER_CONSTELLATIONS * V90DEMAPPER_LEVELS; i++)\n\t\tconstellation[0][i] = 0;\n\tthis->params = params;\n\terrorHistogramCount = 0;",
  "why": "Re-anchored by finding F7793: the constructor's eight zero stores are written high-to-low, which is the object's own emission order, and the two trailing stores are `params` then `errorHistogramCount`.  The mutation is unchanged."
 }
]
