[
 {
  "label": "the gate is not tested, so the detector is polled every step",
  "find": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {",
  "replace": "\tif (detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {",
  "fn": "v34handshak"
 },
 {
  "label": "the gate is inverted",
  "find": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {",
  "replace": "\tif (T3M_I32(&frame, T3M_FSKGATE) == 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {",
  "fn": "v34handshak"
 },
 {
  "label": "the gate is read as a short, so 0x10000 reads clear",
  "find": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {",
  "replace": "\tif (T3M_I16(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {",
  "fn": "v34handshak"
 },
 {
  "label": "the action block runs whenever the gate is set",
  "find": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {",
  "replace": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && (detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4), 1)) {",
  "fn": "v34handshak"
 },
 {
  "label": "the detector is polled over two bins and not three",
  "find": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {",
  "replace": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, 2, fskin, 4)) {",
  "fn": "v34handshak"
 },
 {
  "label": "the detector is handed two samples and not four",
  "find": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {",
  "replace": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 2)) {",
  "fn": "v34handshak"
 },
 {
  "label": "the detector is handed eight samples and not four",
  "find": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {",
  "replace": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 8)) {",
  "fn": "v34handshak"
 },
 {
  "label": "the detector is handed the receiver rather than its samples",
  "find": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {",
  "replace": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS,\n\t\t\t\t (const short *)frame.rx, 4)) {",
  "fn": "v34handshak"
 },
 {
  "label": "the gate returns instead of falling through",
  "find": "\tframe.nbits = obj->fsk.nbits;\n\n\tfskdemodulate(obj, fskin, &obj->fsk);",
  "replace": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0)\n\t\treturn;\n\n\tframe.nbits = obj->fsk.nbits;\n\n\tfskdemodulate(obj, fskin, &obj->fsk);",
  "fn": "v34handshak"
 },
 {
  "label": "the action block returns instead of falling through",
  "find": "\t\tobj->fsk.nbits = 0;\t\t\t/* +0xaae0 */\n\t}",
  "replace": "\t\tobj->fsk.nbits = 0;\t\t\t/* +0xaae0 */\n\t\treturn;\n\t}",
  "fn": "v34handshak"
 },
 {
  "label": "the action block does not print what the object prints",
  "find": "\t\tif (DSPLIB_DEBUG_ON())\n\t\t\tdsplibs_debug_printf(\"DET_SYNC : retrain request \"\n\t\t\t\t\t     \"detected while searching for \"\n\t\t\t\t\t     \"info1\\n\");\n",
  "replace": "",
  "fn": "v34handshak"
 },
 {
  "label": "the action block does not clear the gate",
  "find": "\t\tT3M_I32(&frame, T3M_FSKGATE) = 0;\t/* 0x6938c, 32-bit */\n",
  "replace": "",
  "fn": "v34handshak"
 },
 {
  "label": "the gate is cleared sixteen bits wide",
  "find": "\t\tT3M_I32(&frame, T3M_FSKGATE) = 0;\t/* 0x6938c, 32-bit */",
  "replace": "\t\tT3M_I16(&frame, T3M_FSKGATE) = 0;",
  "fn": "v34handshak"
 },
 {
  "label": "the detector is not re-armed",
  "find": "\t\tdftRetrainDetInit(obj);\n",
  "replace": "",
  "fn": "v34handshak"
 },
 {
  "label": "+0x3588 is stored rather than or-ed",
  "find": "\t\tT3M_I16(&frame, T3M_F3588) =\n\t\t\t(short)(T3M_U16(&frame, T3M_F3588) | 2);",
  "replace": "\t\tT3M_I16(&frame, T3M_F3588) = 2;",
  "fn": "v34handshak"
 },
 {
  "label": "+0x3588 takes bit 0 rather than bit 1",
  "find": "\t\tT3M_I16(&frame, T3M_F3588) =\n\t\t\t(short)(T3M_U16(&frame, T3M_F3588) | 2);",
  "replace": "\t\tT3M_I16(&frame, T3M_F3588) =\n\t\t\t(short)(T3M_U16(&frame, T3M_F3588) | 1);",
  "fn": "v34handshak"
 },
 {
  "label": "+0x3588 is not touched at all",
  "find": "\t\tT3M_I16(&frame, T3M_F3588) =\n\t\t\t(short)(T3M_U16(&frame, T3M_F3588) | 2);\n",
  "replace": "",
  "fn": "v34handshak"
 },
 {
  "label": "the microstate is not moved",
  "find": "\t\ths_setstate(obj, HS_MICROSTATE, V34HS_TX_PHASE1_ANS);\t/* 46 */\n",
  "replace": "",
  "fn": "v34handshak"
 },
 {
  "label": "the microstate is moved to 47 and not 46",
  "find": "\t\ths_setstate(obj, HS_MICROSTATE, V34HS_TX_PHASE1_ANS);\t/* 46 */",
  "replace": "\t\ths_setstate(obj, HS_MICROSTATE, V34HS_TX_PHASE2_ANS);",
  "fn": "v34handshak"
 },
 {
  "label": "the rxstate transition is dropped",
  "find": "\t\ths_setstate(obj, HS_RXSTATE, V34HS_RX_DPSK);\t\t/* 43 */\n",
  "replace": "",
  "fn": "v34handshak",
  "equivalent": true,
  "why": "UNREACHABLE CODE, in ours AND in the blob -- finding 722.  0x64a64 is reached only through `cmp $0x2b,%eax; je 64a64` at 0x62a09 and 0x6754b only from inside that block, and nothing between them writes +0x3594: finding 285 swept `V34agc` and `fskdemodulate` over the whole of .text, and the arm itself writes +0x3592, +0x3588, +0xa8a0, the three bins and the five retrain scalars.  So the rxstate is 43 at 0x694af on every path that can get there, `hs_setstate` finds `now == next` and returns without printing or storing, and deleting the call cannot change a byte or a line.  The object carries the untaken side anyway -- 0x694bc-0x69535 is a debug test, a 94-byte printf body and a `movw $0x2b,0x3594` that cannot execute -- because GCC 3.4 inlined `hs_setstate` and could not see the caller's invariant.  This is finding 718's first kind: the mutated line cannot execute at the one input that would matter, so no fixture can catch it.  The two mutations below, which change the transition's ARGUMENTS rather than deleting it, ARE caught -- a different target makes `now != next` and the store happens."
 },
 {
  "label": "the rxstate transition goes to 4 RECEIVE",
  "find": "\t\ths_setstate(obj, HS_RXSTATE, V34HS_RX_DPSK);\t\t/* 43 */",
  "replace": "\t\ths_setstate(obj, HS_RXSTATE, V34HS_RECEIVE);",
  "fn": "v34handshak"
 },
 {
  "label": "the rxstate transition writes the microstate word",
  "find": "\t\ths_setstate(obj, HS_RXSTATE, V34HS_RX_DPSK);\t\t/* 43 */",
  "replace": "\t\ths_setstate(obj, HS_MICROSTATE, V34HS_RX_DPSK);",
  "fn": "v34handshak"
 },
 {
  "label": "the txstate is not moved",
  "find": "\t\ths_setstate(obj, HS_TXSTATE, V34HS_TONE_AB);\t\t/* 60 */\n",
  "replace": "",
  "fn": "v34handshak"
 },
 {
  "label": "the txstate is moved to 61 and not 60",
  "find": "\t\ths_setstate(obj, HS_TXSTATE, V34HS_TONE_AB);\t\t/* 60 */",
  "replace": "\t\ths_setstate(obj, HS_TXSTATE, 61);",
  "fn": "v34handshak"
 },
 {
  "label": "the txstate transition is taken before the microstate one",
  "find": "\t\ths_setstate(obj, HS_MICROSTATE, V34HS_TX_PHASE1_ANS);\t/* 46 */\n\t\ths_setstate(obj, HS_RXSTATE, V34HS_RX_DPSK);\t\t/* 43 */\n\t\ths_setstate(obj, HS_TXSTATE, V34HS_TONE_AB);\t\t/* 60 */",
  "replace": "\t\ths_setstate(obj, HS_TXSTATE, V34HS_TONE_AB);\n\t\ths_setstate(obj, HS_MICROSTATE, V34HS_TX_PHASE1_ANS);\n\t\ths_setstate(obj, HS_RXSTATE, V34HS_RX_DPSK);",
  "fn": "v34handshak"
 },
 {
  "label": "+0x358c is compared signed",
  "find": "\t\tif (T3M_U16(&frame, T3M_TOGGLE) > 1)",
  "replace": "\t\tif (T3M_I16(&frame, T3M_TOGGLE) > 1)",
  "fn": "v34handshak"
 },
 {
  "label": "+0x358c is cleared whenever it is non-zero",
  "find": "\t\tif (T3M_U16(&frame, T3M_TOGGLE) > 1)",
  "replace": "\t\tif (T3M_U16(&frame, T3M_TOGGLE) > 0)",
  "fn": "v34handshak"
 },
 {
  "label": "+0x358c is cleared at 1 as well",
  "find": "\t\tif (T3M_U16(&frame, T3M_TOGGLE) > 1)",
  "replace": "\t\tif (T3M_U16(&frame, T3M_TOGGLE) >= 1)",
  "fn": "v34handshak"
 },
 {
  "label": "+0x358c is set to 1 rather than cleared",
  "find": "\t\t\tT3M_U16(&frame, T3M_TOGGLE) = 0;",
  "replace": "\t\t\tT3M_U16(&frame, T3M_TOGGLE) = 1;",
  "fn": "v34handshak"
 },
 {
  "label": "+0x358c is cleared unconditionally",
  "find": "\t\tif (T3M_U16(&frame, T3M_TOGGLE) > 1)\n\t\t\tT3M_U16(&frame, T3M_TOGGLE) = 0;",
  "replace": "\t\tT3M_U16(&frame, T3M_TOGGLE) = 0;",
  "fn": "v34handshak"
 },
 {
  "label": "vect_idx is not cleared",
  "find": "\t\tobj->vect_idx = 0;\t\t\t/* +0x2aa2 */\n",
  "replace": "",
  "fn": "v34handshak"
 },
 {
  "label": "+0xaa78 is not cleared",
  "find": "\t\tT3M_U16(&frame, T3M_COUNTER) = 0;\t/* +0xaa78 */\n",
  "replace": "",
  "fn": "v34handshak"
 },
 {
  "label": "fsk.sr is cleared rather than set to -1",
  "find": "\t\tobj->fsk.sr = -1;\t\t\t/* +0xaae2 */",
  "replace": "\t\tobj->fsk.sr = 0;",
  "fn": "v34handshak"
 },
 {
  "label": "fsk.sr is not touched",
  "find": "\t\tobj->fsk.sr = -1;\t\t\t/* +0xaae2 */\n",
  "replace": "",
  "fn": "v34handshak"
 },
 {
  "label": "fsk.nbits is not cleared",
  "find": "\t\tobj->fsk.nbits = 0;\t\t\t/* +0xaae0 */\n",
  "replace": "",
  "fn": "v34handshak"
 },
 {
  "label": "the two fsk fields are swapped",
  "find": "\t\tobj->fsk.sr = -1;\t\t\t/* +0xaae2 */\n\t\tobj->fsk.nbits = 0;\t\t\t/* +0xaae0 */",
  "replace": "\t\tobj->fsk.nbits = -1;\n\t\tobj->fsk.sr = 0;",
  "fn": "v34handshak"
 },
 {
  "label": "the samples come from the receiver's base",
  "find": "\tfskin = (const short *)((unsigned char *)frame.rx + T3M_RX_FSKIN);",
  "replace": "\tfskin = (const short *)((unsigned char *)frame.rx);",
  "fn": "v34handshak"
 },
 {
  "label": "nbits is read before the gate rather than after it",
  "find": "\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {\n\t\t/*\n\t\t * 0x6936e.  The action block: the far end asked for a\n\t\t * retrain while this end was still looking for INFO1.\n\t\t */\n\t\tif (DSPLIB_DEBUG_ON())\n\t\t\tdsplibs_debug_printf(\"DET_SYNC : retrain request \"\n\t\t\t\t\t     \"detected while searching for \"\n\t\t\t\t\t     \"info1\\n\");\n\n\t\tT3M_I32(&frame, T3M_FSKGATE) = 0;\t/* 0x6938c, 32-bit */\n\n\t\t/*\n\t\t * 0x69392-0x69425, and it is `dftRetrainDetInit` inlined:\n\t\t * thirteen store sites at identical offsets, widths and\n\t\t * constants against the standalone copy at 0x5ea60 --\n\t\t * +0x28/+0x2a/+0x00/+0x04/+0x08 over three bins at stride\n\t\t * 0x2c, the three phase steps 0x600/0x800/0xa00, and the\n\t\t * five scalars at +0xa24a.  +0xa24c is the only 32-bit store\n\t\t * on either side.  Finding 720.\n\t\t */\n\t\tdftRetrainDetInit(obj);\n\n\t\t/* 0x693fa/0x69411/0x69425, a 16-bit read-modify-write. */\n\t\tT3M_I16(&frame, T3M_F3588) =\n\t\t\t(short)(T3M_U16(&frame, T3M_F3588) | 2);\n\n\t\t/*\n\t\t * Three transitions, 0x6941a, 0x694b6 and 0x69544, each the\n\t\t * compare-print-store idiom with the second `%s` folded to\n\t\t * its own `StateName` entry: .data+0x6cb8, +0x6cac and\n\t\t * +0x6cf0 are indices 46, 43 and 60.\n\t\t *\n\t\t * THE MIDDLE ONE CAN NEVER FIRE.  0x64a64 is reached only\n\t\t * through `cmp $0x2b,%eax; je 64a64` at 0x62a09 and 0x6754b\n\t\t * only from inside that, so the rxstate is 43 on every path\n\t\t * that gets here -- the object's `je 69536` at 0x694ba\n\t\t * always takes and 0x694bc-0x69535 is dead in the blob too.\n\t\t * It is written because it is what the object has.  Finding\n\t\t * 722.\n\t\t */\n\t\ths_setstate(obj, HS_MICROSTATE, V34HS_TX_PHASE1_ANS);\t/* 46 */\n\t\ths_setstate(obj, HS_RXSTATE, V34HS_RX_DPSK);\t\t/* 43 */\n\t\ths_setstate(obj, HS_TXSTATE, V34HS_TONE_AB);\t\t/* 60 */\n\n\t\t/* 0x695cb is `cmpw $0x1` then `jbe`, so UNSIGNED. */\n\t\tif (T3M_U16(&frame, T3M_TOGGLE) > 1)\n\t\t\tT3M_U16(&frame, T3M_TOGGLE) = 0;\n\n\t\t/* 0x695f0..0x69605, in the object's order. */\n\t\tobj->vect_idx = 0;\t\t\t/* +0x2aa2 */\n\t\tT3M_U16(&frame, T3M_COUNTER) = 0;\t/* +0xaa78 */\n\t\tobj->fsk.sr = -1;\t\t\t/* +0xaae2 */\n\t\tobj->fsk.nbits = 0;\t\t\t/* +0xaae0 */\n\t}\n\n\t/*\n\t * 0x64a9e, and it has to be read HERE: `fskdemodulate` writes it --\n\t * and so does the action block above, which zeroes it.  Both orders\n\t * are observable and this is the object's.\n\t */\n\tframe.nbits = obj->fsk.nbits;\n",
  "replace": "\tframe.nbits = obj->fsk.nbits;\n\n\tif (T3M_I32(&frame, T3M_FSKGATE) != 0\n\t    && detectRetrainReq(obj, V34_RETRAIN_BINS, fskin, 4)) {\n\t\t/*\n\t\t * 0x6936e.  The action block: the far end asked for a\n\t\t * retrain while this end was still looking for INFO1.\n\t\t */\n\t\tif (DSPLIB_DEBUG_ON())\n\t\t\tdsplibs_debug_printf(\"DET_SYNC : retrain request \"\n\t\t\t\t\t     \"detected while searching for \"\n\t\t\t\t\t     \"info1\\n\");\n\n\t\tT3M_I32(&frame, T3M_FSKGATE) = 0;\t/* 0x6938c, 32-bit */\n\n\t\t/*\n\t\t * 0x69392-0x69425, and it is `dftRetrainDetInit` inlined:\n\t\t * thirteen store sites at identical offsets, widths and\n\t\t * constants against the standalone copy at 0x5ea60 --\n\t\t * +0x28/+0x2a/+0x00/+0x04/+0x08 over three bins at stride\n\t\t * 0x2c, the three phase steps 0x600/0x800/0xa00, and the\n\t\t * five scalars at +0xa24a.  +0xa24c is the only 32-bit store\n\t\t * on either side.  Finding 720.\n\t\t */\n\t\tdftRetrainDetInit(obj);\n\n\t\t/* 0x693fa/0x69411/0x69425, a 16-bit read-modify-write. */\n\t\tT3M_I16(&frame, T3M_F3588) =\n\t\t\t(short)(T3M_U16(&frame, T3M_F3588) | 2);\n\n\t\t/*\n\t\t * Three transitions, 0x6941a, 0x694b6 and 0x69544, each the\n\t\t * compare-print-store idiom with the second `%s` folded to\n\t\t * its own `StateName` entry: .data+0x6cb8, +0x6cac and\n\t\t * +0x6cf0 are indices 46, 43 and 60.\n\t\t *\n\t\t * THE MIDDLE ONE CAN NEVER FIRE.  0x64a64 is reached only\n\t\t * through `cmp $0x2b,%eax; je 64a64` at 0x62a09 and 0x6754b\n\t\t * only from inside that, so the rxstate is 43 on every path\n\t\t * that gets here -- the object's `je 69536` at 0x694ba\n\t\t * always takes and 0x694bc-0x69535 is dead in the blob too.\n\t\t * It is written because it is what the object has.  Finding\n\t\t * 722.\n\t\t */\n\t\ths_setstate(obj, HS_MICROSTATE, V34HS_TX_PHASE1_ANS);\t/* 46 */\n\t\ths_setstate(obj, HS_RXSTATE, V34HS_RX_DPSK);\t\t/* 43 */\n\t\ths_setstate(obj, HS_TXSTATE, V34HS_TONE_AB);\t\t/* 60 */\n\n\t\t/* 0x695cb is `cmpw $0x1` then `jbe`, so UNSIGNED. */\n\t\tif (T3M_U16(&frame, T3M_TOGGLE) > 1)\n\t\t\tT3M_U16(&frame, T3M_TOGGLE) = 0;\n\n\t\t/* 0x695f0..0x69605, in the object's order. */\n\t\tobj->vect_idx = 0;\t\t\t/* +0x2aa2 */\n\t\tT3M_U16(&frame, T3M_COUNTER) = 0;\t/* +0xaa78 */\n\t\tobj->fsk.sr = -1;\t\t\t/* +0xaae2 */\n\t\tobj->fsk.nbits = 0;\t\t\t/* +0xaae0 */\n\t}\n\n\t/*\n\t * 0x64a9e, and it has to be read HERE: `fskdemodulate` writes it --\n\t * and so does the action block above, which zeroes it.  Both orders\n\t * are observable and this is the object's.\n\t */\n",
  "fn": "v34handshak",
  "equivalent": true,
  "why": "UNOBSERVABLE THROUGH THIS GATE, and the argument is the object's.  0x64a9e loads `fsk.nbits` into %ebx, which is callee-saved and survives `fskdemodulate`; the ONLY instruction in `v34handshak` that consumes it is arm 55's first guard at 0x65b79, which compares the field against it sixteen bits wide (finding 430, and `frame.nbits` exists in this reconstruction for that guard alone -- `grep -n 'f->nbits' src/pump/v34/v34hshak.c` is the initialiser and `t3m_micro55`, and nothing else).  The gate's action block ends by writing +0x3592 = 46 at 0x6949c, so a step that FIRED can only reach microstate 46's arm at 0x65d6d and never 55's at 0x65b72, and a step that did NOT fire leaves `fsk.nbits` untouched between the two candidate load points.  So the load's position relative to 0x64a87 changes no byte and no line on any path, in the blob as much as here.  THE LIMIT, and it is why the placement is still written the object's way: the moment the action block's target microstate changes, or 46's arm is found to read the pre-demodulator count, this becomes catchable and the flag must come off.  It is finding 714's third kind -- an equivalence that holds because of what the reachable arms do, not because of what C means."
 }
]
