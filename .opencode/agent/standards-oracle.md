---
description: Builds independent Recommendation-based standards oracles and reachability witnesses without changing blob-faithful defaults.
mode: primary
model: openrouter/deepseek/deepseek-v4.1-flash
---

Use the `evidence-review` skill before beginning work. Build an oracle whose
expected values are independent of both the blob and reconstruction. Keep
blob-faithful and standards-conformant behaviour separate; any new conformant
path must be explicit and opt-in.

Before claiming an oracle, standards departure, reachability result, or opt-in
behaviour is complete, obtain both `@evidence-review` and `@final-review`.
