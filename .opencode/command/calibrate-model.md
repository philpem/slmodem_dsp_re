---
description: Run a controlled model-calibration review before expanding a model's role or treating it as the best choice.
agent: build
---

Run a read-only calibration against the same bounded artifact and score the
candidate on the project rubric: preserves the source x profile premise,
detects invalid controls and missing denominators, reports gains and losses,
proposes a discriminator rather than a nearby retry, and avoids unsupported
retention. Record the model ID, prompt, artifact identity, output, and score.

$ARGUMENTS
