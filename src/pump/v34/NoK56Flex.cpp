/*
 * NoK56Flex.cpp -- the empty translation unit the object records.
 *
 * dsplibs.o carries a FILE record `NoK56Flex.cpp` with no symbols attributed
 * to it, in the same way it carries an empty `fax.c`; whatever the original
 * defined there produced no symbol of its own.  This file exists only to emit
 * the same FILE record in the same position, and is deliberately empty.
 */
